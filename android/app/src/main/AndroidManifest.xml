<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Microphone access for on-device voice dictation. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Network is used ONLY to download the on-device AI models once.
         After setup the app works fully offline. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Rewrite history, settings and downloaded models never leave the device,
         so they must not be swept into Google's cloud backup either. Without
         android:allowBackup="false" the platform default is true, which would
         upload rescripto.db and the preferences to the user's Drive. The two
         rule files below are belt and braces: API 31+ honours
         dataExtractionRules for device transfer independently of allowBackup.

         The trade: history and settings no longer survive a device migration.
         That is what the in-app encrypted backup is for. -->
    <application
        android:label="Rescripto"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
